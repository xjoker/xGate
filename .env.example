# xGate 不再读取环境变量。所有配置请编辑 data/config/mini.toml。
#
# 仅 docker-compose 本身使用以下变量：
#   SERVER_PORT  → 宿主机暴露端口（容器内固定监听 8024）
#
# 复制 data/config/mini.toml.example 为 data/config/mini.toml 后修改即可。

SERVER_PORT=8024
